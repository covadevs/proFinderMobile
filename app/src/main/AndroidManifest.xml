<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="profinder.com.br.profindermobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/proFinder"
            android:theme="@style/apptheme" />
        <activity
            android:name=".CadastroUsuarioActivity"
            android:theme="@style/apptheme" />
        <activity
            android:name=".CadastroProjetoActivity"
            android:theme="@style/apptheme" />
        <activity
            android:name=".ProfessorActivity"
            android:label="@string/title_activity_professor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".EditarProjetoActivity"
            android:theme="@style/apptheme"></activity>
        <activity
            android:name=".ConfiguracoesActivity"
            android:theme="@style/apptheme"></activity>
        <activity
            android:name=".BuscaAcitvity"
            android:label="@string/title_activity_busca_acitvity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>